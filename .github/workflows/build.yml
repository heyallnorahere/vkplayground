name: build
on: [ push ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v2
        with:
          submodules: recursive
      - name: install dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install vulkan-tools libvulkan-dev vulkan-validationlayers-dev spirv-tools libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libgl1-mesa-dev
      - name: configure project
        shell: bash
        run: |
          ./config.sh
      - name: build project
        shell: bash
        run: |
          make -C build -j 8